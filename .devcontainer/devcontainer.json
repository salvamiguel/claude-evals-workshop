{
  "name": "Claude Evals Workshop",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "features": {
    "ghcr.io/devcontainers/features/node:1": { "version": "20" }
  },
  "postCreateCommand": "pip install anthropic python-dotenv pyyaml jupyter notebook rise && ([ -f dashboard/package.json ] && (cd dashboard && npm install) || echo 'dashboard not yet scaffolded, skipping npm install')",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-toolsai.jupyter",
        "ms-toolsai.vscode-jupyter-slideshow",
        "ms-python.python",
        "Vue.volar"
      ]
    }
  }
}
